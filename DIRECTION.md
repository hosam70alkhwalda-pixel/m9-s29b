# Direction declaration

Edit this file to declare which direction you are pursuing for this
stretch. The autograder reads the first non-comment, non-blank line and
dispatches to the matching test module.

Choices:
- `a` — Explainability (you implement `extension_a/trace.py`)
- `b` — Robustness (you implement `extension_b/fallback.py`)

Replace the line below with a single letter (`a` or `b`) on its own line.

UNDECLARED
